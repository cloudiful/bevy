[workspace]
members = ["outline", "camera", "input", "localization", "settings"]
resolver = "2"

[workspace.dependencies]
bevy = { version = "0.19.0", default-features = false }
serde = { version = "1.0.228", features = ["derive"] }
toml = "1.1.2"
